{% extends '@Bootstrap/Dashboard.twig' %}

{% block title %}Modifier la News{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Modifier la News</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">Enregistrer</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_news_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
